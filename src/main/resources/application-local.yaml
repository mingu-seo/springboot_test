spring:
  datasource:
    # Docker Compose MariaDB (infra/docker-compose.db.yml) 가 3307 포트로 노출됨
    # 로컬 MariaDB(3306) 를 쓰고 싶으면 아래 url 의 포트만 3306 으로 바꾸세요.
    url: jdbc:mariadb://localhost:3307/springstudy
    username: testuser
    password: test1234

jwt:
  member:
    secret: shinhanswacademyshinhanswacademy